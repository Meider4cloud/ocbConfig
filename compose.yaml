version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - .:/app
    command: ['deno', 'task', 'prod']
    environment:
      # - DIST_MODULE=
      - DIST_NAME="Hello World"
      # - DIST_DESCRIPTION=
      # - DIST_VERSION=
      # - DIST_OUTPUT_PATH=
